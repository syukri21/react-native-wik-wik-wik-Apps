import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container : (width) => ({
		width          : width,
		height         : width,
		justifyContent : 'center',
		alignItems     : 'center'
	})
});

export { styles };
